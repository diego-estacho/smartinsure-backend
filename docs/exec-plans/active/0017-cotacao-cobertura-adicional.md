# Exec-plan 0017 — Cotação com Cobertura Adicional: enviar ao PlugV2 o que o corretor escolheu — RN-104/105/106 — AB#0007 (slug `ab-0007-cotacao-cobertura-adicional`)

Status: **ativo** (2026-08-04). Atividade **cross-repo, backend primeiro** (toca prêmio e contrato): worktrees `C:\wt\ab-0007\{smartinsure-backend,smartinsure-frontend,smartinsure-dbmigration}`, branch `ab-0007-cotacao-cobertura-adicional` nos três. RN-104/105/106 catalogadas como **proposta — aguardando aprovação da PO**; o código fica na branch até essa aprovação.

Contexto obrigatório (ler antes de executar): `AGENTS.md`; RNs `coberturas-adicionais.md` (RN-040..046, em especial **RN-046** — oferta pela canônica com disponibilidade derivada dos vínculos), `grupo-de-cotacao.md` (RN-050/051/102/**104**), `cotacao.md` (RN-056..063, **105**, **106**, RN-103); **ADR-103** (envio pelo nome da Importada — a decisão central, com a evidência do probe), ADR-064 (classificação do resultado), ADR-045/ADR-028 (a ACL não vaza modelo de fornecedor nem hospeda regra), ADR-041..043 (Flyway dono do schema, forward-only), ADR-029/030 (EntityBase, UUIDv7), ADR-031 (enum como string), ADR-034 (FK Restrict), **OPEN-22** (nome divergente por ramo) e **OPEN-16** (semântica do tipo de cálculo — mantém valor segurado por cobertura fora de escopo).

**Origem:** investigação de 2026-08-04. `QuotationRequestProcessor` enviava `AdditionalCoverages` **hardcoded vazio**, com `TODO(probe T14)`: toda Cotação era precificada só com a garantia principal, independentemente do que o corretor marcasse na etapa 3, e nada avisava o usuário. O Grupo guardava apenas dois booleanos provisórios (`IncludesPenaltyCoverage`/`IncludesLaborCoverage`), incapazes de representar o catálogo real.

## Objetivo

Fazer a Cotação chegar à Seguradora **com as Coberturas Adicionais que o corretor escolheu**. A escolha passa a ser relação com a Cobertura Adicional **canônica** (os dois booleanos saem do domínio e do contrato); no fan-out, um resolvedor na Application traduz canônica → **nome** da Importada daquela Seguradora/Modalidade (ADR-103) e devolve o que ela não oferece; cada Cotação registra a situação de cada cobertura (**Enviada** / **Não contemplada**) e a comparação sinaliza a lacuna, para que prêmios de escopos diferentes nunca sejam comparados sem aviso. Seguradora que não oferece a cobertura **é cotada mesmo assim, sem ela** (decisão de 2026-08-04).

## Tarefas (branch `ab-0007-cotacao-cobertura-adicional`)

- [x] **T1 — RNs e decisão aberta.** RN-104 (`grupo-de-cotacao.md`), RN-105/RN-106 (`cotacao.md`), OPEN-22 (`open-decisions.md`), marcador "Situação da Cobertura Adicional na Cotação" no glossário. **Gate:** aprovação da PO antes do merge.
- [x] **T2 — ADR-103.** Envio pelo nome da Importada, com a evidência do probe (GUID recusado com erro nomeando o valor; 400 derruba a Cotação inteira; dedup ignora IS e vigência).
- [x] **T3 — Este exec-plan.**
- [x] **T4 — Migrations (`smartinsure-dbmigration`), somente DDL.** Cria `QuotationGroupAdditionalCoverages` e `QuotationAdditionalCoverages`; **derruba** `IncludesPenaltyCoverage`/`IncludesLaborCoverage` de `QuotationGroups`. **Nenhum dado é tocado por migration** (decisão de 2026-08-04): o catálogo de Coberturas Adicionais canônicas é curado por ambiente (RN-040), então nome, id e existência diferem entre dev, QA e produção — migration que resolvesse canônica por nome converteria errado ou abortaria o deploy. **Consequência assumida:** o valor dos booleanos é perdido no DROP; onde precisar ser preservado, a conversão é passo de operação no ambiente, antes desta migration, por quem conhece o catálogo dali.
- [x] **T5+T9 — Domínio e contrato (atômico).** `EQuotationAdditionalCoverageStatus`, as duas entidades, `IQuotationAdditionalCoverageResolver` + records em Core; `QuotationGroup` sem booleanos e com a coleção; `Quotation.RecordAdditionalCoverages`; mappings e DbSets; e no **mesmo commit** request/response/use cases/endpoint com `additionalCoverageIds` (breaking).
- [x] **T6 — Consultas.** `ListForQuotationAsync` (por Seguradora/Modalidade/canônicas) e `ListAvailableForModalityAsync` (união simples por Corretora do Escopo ativo, exigindo **Seguradora Ativa** além de Habilitação Ativa), com o **mesmo critério de derivação** para oferta e envio nunca divergirem. A escolha é validada contra essa mesma consulta na criação/atualização do Grupo (`AdditionalCoverageSelectionRules`) — o servidor recusa cobertura indisponível para a Modalidade, então a limpeza no cliente é conveniência, não garantia.
- [x] **T7 — Resolvedor (TDD).** `|N|`=1 envia; `|N|`=0 não contempla; `|N|`>1 não contempla (OPEN-22); ramos com nome igual enviam uma vez; Grupo sem cobertura não consulta catálogo.
- [x] **T8 — Fan-out (TDD).** `QuotationRequestProcessor` envia os nomes resolvidos e grava a situação **assim que resolvida, antes até de resolver o motor**, para o registro sobreviver a Indisponível (RN-058) e a falha de integração (RN-057). Fecha o `TODO(probe T14)`.
- [x] **T10 — Endpoint do corretor.** `GET /api/v1/modalities/{id}/additional-coverages`, autorização default (o `/additional-coverages/map` existente é restrito a Administrador e não serve).
- [x] **T11 — Leitura das Cotações.** `additionalCoverages: [{ additionalCoverageId, name, status, sentName? }]` — `name` é o da canônica; `sentName` só quando `Sent`.
- [x] **T12 — Gates do backend.** PR **não aberto** — aguardando aprovação da PO das RNs.
- [x] **T13..T16 — Front** (E2E e screenshot pendentes, ver Evidências)**.** Rota no BFF (ADR-008) + composable `useAvailableAdditionalCoverages`; store com `additionalCoverageIds` e **limpeza ao trocar Modalidade**; etapa 3 dinâmica (saiu o par de checkboxes fixos), com estados de vazio/loading/erro; marcador de cobertura não contemplada na comparação (tabela e cards), filtrado por **nome estável** do status; gates verdes.

## Critérios de aceite

1. Cotação solicitada com cobertura escolhida chega à Seguradora com o **nome** da Importada em `AdditionalCoverages`; nunca com o `SourceUniqueId` (ADR-103).
2. Uma canônica escolhida contribui com **exatamente um** nome; a lista vai sem repetição; Grupo sem escolha envia `[]`.
3. Seguradora que não oferece a cobertura **é cotada**, e a Cotação registra a cobertura como **Não contemplada** — inclusive quando a Cotação resulta Indisponível ou falha na integração.
4. Nome divergente entre ramos da mesma Seguradora **não é enviado** e consta como Não contemplada (OPEN-22) — nunca se envia superset.
5. A etapa 3 oferece apenas canônicas Ativas com vínculo ativo na Modalidade escolhida, nas Seguradoras habilitadas da Corretora do Escopo ativo; trocar a Modalidade descarta a seleção.
6. Os dois booleanos saem do domínio, do banco e do contrato; Grupos existentes são convertidos sem perder seleção.
7. A comparação sinaliza cada cobertura não contemplada, por **nome estável** do status.
8. `dotnet build SmartInsure.slnx` sem erro e `dotnet test` sem falha; `check-harness.py` sem violação **nova**; front `lint`/`typecheck`/`test`/`build` verdes. **A cobertura global de 80% não é critério desta entrega** — já falhava na `main` (58,81%) antes dela; o critério aqui é não regredir a cobertura e manter as classes novas a 100%. E2E da jornada depende da curadoria dos vínculos (ver Evidências).

## Evidências

- **Importação de Coberturas Adicionais rodada** (2026-08-04, banco local, Habilitações da Finn apontando para o gateway QA, `POST /additional-coverage-imports/run`): `modalitiesProcessed: 216, modalitiesSucceeded: 216, modalitiesFailed: 0`, 88s → **249 Coberturas Adicionais Importadas**, 25 nomes distintos, `SourceUniqueId` em **100%** das linhas, **0 vínculos** com a canônica (curadoria é manual — RN-043).
- **Probe T14 do `POST /Cotation`** (gateway QA, corretora Finn, modalidade Licitante, 2026-08-04):
  - `["b4e65794-032b-4210-bfc4-6eef35210833"]` (SourceUniqueId) → **HTTP 400** `"Atenção! Existem coberturas informadas na criação da cotação que não são suportadas: b4e65794-…"`
  - `["Multas"]` (nome da Importada) → **HTTP 200**, `ResponseStatus.Status = 5`
  - Cobertura não suportada **derruba a solicitação inteira**; o dedup do gateway ignora variação de IS e de vigência.
  - **Aplicação confirmada depois, no banco da Seguradora** (ver item próprio abaixo) — o probe só provava aceitação.
- **Backend (2026-08-04):** `dotnet build SmartInsure.slnx` → **0 erros** (50 warnings, todos pré-existentes). `dotnet test tests/SmartInsure.Tests` → **668 testes, 0 falhas** (`main` tinha 646 — **+22** nesta atividade). Testes novos carregam o ID da RN: RN-104 (5 na entidade + 5 no endpoint), RN-105/RN-106 (7 no resolvedor + 3 no processor + 2 na leitura do leque).
- **Cobertura das classes novas: 100%** — `QuotationAdditionalCoverageResolver`, `ListAvailableAdditionalCoveragesUseCase`, `QuotationAdditionalCoverage`, `QuotationGroupAdditionalCoverage` e os dois mappings.
- **Condições pré-existentes registradas (nenhuma criada por esta atividade):**
  1. `check-harness.py` acusava, na `main`, **RN-062 e RN-063 duplicados** entre `cotacao.md` e `perfis-e-permissoes.md`. **Resolvido na `main`** e incorporado no merge de 2026-08-04 — o lint agora passa (`harness ok`).
  2. **A cobertura global já está abaixo do gate de 80%:** `main` = **58,81%**; esta branch = **59,50%** (+0,69pp). O gate de CI, portanto, já falhava antes desta atividade. Precisa de uma frente própria de cobertura.
- **Front (2026-08-04):** `pnpm lint` → OK; `pnpm typecheck` → OK; `pnpm test` → **344 testes, 0 falhas** (antes 339 — **+5**); `pnpm build` → OK (11,3 MB / 2,97 MB gzip).
- **Ressalva do contrato:** `pnpm types:gen` foi rodado com `OPENAPI_CONTRACT` apontando para o documento OpenAPI servido pela **API local** desta worktree, porque `docs/generated/openapi.json` é produzido pelo CI e ainda não reflete este trabalho. Nenhum type foi editado à mão. **Antes do merge do front, regerar de `docs/generated/openapi.json` publicado pelo CI** (após o merge do backend) e conferir que o diff de `app/types/gen/api.ts` fica vazio.
- **Revisão adversarial (2026-08-04)** — subagente dedicado achou 12 pontos; os de correção foram corrigidos e reverificados:
  - **Crítico (e depois superado pela decisão de escopo):** a conversão dos booleanos abortaria em QA/produção, porque nada semeia as canônicas — elas nascem da curadoria. Foi corrigida na hora (semeando quando necessário) e testada em banco limpo, mas em 2026-08-04 a **conversão saiu da migration por completo**: manipular dado curado por ambiente não é papel de migration. O achado segue registrado porque foi ele que expôs o acoplamento entre schema e catálogo.
  - **Alto:** o guard `IF COL_LENGTH(...)` **não** protege DML (nome de coluna é resolvido na compilação do batch), só `DROP COLUMN`. Deixou de importar quando o DML saiu da migration, mas vale como nota para quem escrever migration com DML no futuro.
  - **Alto:** `ListAvailableForModalityAsync` não exigia **Seguradora Ativa**, então a etapa 3 ofereceria cobertura que o envio recusa. Corrigido no repositório e em RN-104.
  - **Médio/alto:** corpo sem `additionalCoverageIds` virava **500** (`RuleForEach` ignora coleção nula) — `NotNull()` nos dois validadores.
  - **Médio:** "trocar a Modalidade descarta a seleção" existia **só no cliente** — agora o servidor valida a disponibilidade na criação e na atualização.
  - **Médio:** `QuotationRepository.GetByIdAsync` não carregava a coleção filha que o processor substitui (armadilha de FK Restrict no `Remove`).
  - **Médio:** `docs/generated/openapi.json` ficara stale enquanto os types do front eram regerados. Regenerado; `pnpm types:gen` a partir do arquivo versionado agora produz **diff vazio** em `app/types/gen/api.ts`.
  - **Menores:** registro de cobertura passou a ocorrer antes do `ResolveEngine`; nomes de índice dos mappings alinhados às migrations; RN-106 precisou o caso da Cotação indisponível local (RN-056); fixture morta do teste de BFF atualizada; poda da seleção obsoleta extraída para função pura testável e guard de latest-wins na etapa 3.
  - **Bônus:** `Execute_DeveRecusar_QuandoGrupoJaTemCotacoes` (RN-060) estava **sem `[Fact]`** desde a `main` — nunca rodava. `[Fact]` adicionado e o teste **passa**.
  - Eixos sem defeito: regra dos 7 passos do resolvedor, autorização do endpoint novo, nome canônico vs enviado na resposta, ausência de resquício dos booleanos, nome estável e design tokens no front, ordenação/sintaxe das migrations, convenções de EF.
- **Testes após as correções:** backend **672** (0 falhas); front **348** (0 falhas), `lint`/`typecheck`/`build` verdes.
- **Bug encontrado em teste manual e corrigido (2026-08-04):** o fan-out enviava `AdditionalCoverages: []` mesmo com cobertura escolhida. Causa: `QuotationRequestProcessor` carregava o Grupo por `GetByIdAsync` (→ `Set.FindAsync`, sem `Include`) e lia `group.AdditionalCoverages`, que chega **vazia** (não há lazy loading). Consequência: a cobertura não era enviada e nenhuma situação era gravada — o defeito que RN-105/RN-106 existem para corrigir, reintroduzido por omissão de `Include`. **Nenhum teste pegou**, porque todos mockam o repositório devolvendo um `QuotationGroup` já povoado, o que não modela o EF. Corrigido substituindo a navegação por consulta projetada (`IQuotationGroupRepository.ListAdditionalCoverageIdsAsync`) — sem navegação, não há `Include` a esquecer. O `SetupValidGraph` dos testes passou a criar o Grupo com a navegação **vazia** (como o EF entrega) e alimentar os ids só pela consulta; verifiquei que o teste falha ao reverter o processor para a navegação.
- **Cotação real com cobertura, verificada no `QuotationIntegrationLog` (2026-08-04 14:03, Licitante, Multas escolhida):** a mesma canônica saiu com o nome de cada Seguradora — `["Multas"]` para AXA e Sombrero, **`["Multa"]`** (singular) para Excelsior — e `[]` para as que não oferecem. As 8 Cotações do Grupo gravaram situação: 3 `Sent` (com `SentName` da origem) e 5 `NotOffered`, **inclusive nas que resultaram `Unavailable`**, confirmando o registro antes do `ResolveEngine`. Prêmios: Sombrero R$ 225,00 (com Multas) × SANCOR R$ 250,00 (`NotOffered`) — a comparação de escopos diferentes agora é sinalizada.
- **Aplicação da cobertura CONFIRMADA no lado da Seguradora** (`db-garantia-qa-us`, 2026-08-04), sobre duas propostas do mesmo fan-out, mesmo risco, mesmo instante:
  - **202600000274307** (Sombrero, enviamos `["Multas"]`): `ProposalCoverage` com 1 linha — `Multas`, `IsAdditionalCoverage = true`, `Value` 75,00. Prêmio **225,00 = 150,00 (principal) + 75,00 (cobertura)**.
  - **202600000274306** (SANCOR, enviamos `[]`): **nenhuma** linha de cobertura; prêmio 250,00, só a garantia principal — o `NotOffered` é fiel.
  - O `Coverage.UniqueId` gravado na proposta é `1B968A60-…-82136ED7A6E6`, o identificador da "Multas" da Sombrero no ramo **Public** — o MESMO GUID que o gateway recusou no probe. Enviamos o nome e a Seguradora resolveu o ramo correto (Segurado é município): a consequência central do ADR-103 passa de inferência a medição.
  - Fecha o critério de aceite 1 com evidência do outro lado da integração, não só do payload.
- **Dívida de teste registrada:** o projeto de testes não tem provider de EF (só NSubstitute), então omissão de `Include`/`Select` em repositório é estruturalmente invisível. Fechar exige teste de repositório com provider real — frente própria.
- [ ] Aprovação da PO das RN-104/105/106 (e ciência de OPEN-22) — **pendente**.
- [ ] PRs (backend → dbmigration → front) — **não abertos**, aguardando a aprovação acima.
- [ ] E2E da jornada e screenshot da etapa 3 / do marcador na comparação — **pendente**: exige curadoria dos vínculos (0 hoje), senão a etapa 3 lista vazio.
- [ ] Gates do front + screenshot da etapa 3 e do marcador na comparação — **pendente (T16)**.
- **Curadoria dos vínculos feita (2026-08-04, banco local, pela API — RN-043, não por UPDATE direto):** das 249 Coberturas Adicionais Importadas, **182 vinculadas** e **67 Ignoradas**, **zero pendentes** — o catálogo importado está inteiramente curado no ambiente local. De-para aplicado: `Multas`/`Multa` → **Multas** (83 linhas, 7 Seguradoras); `Trabalhista e Previdenciário`/`Ações Trabalhistas e Previdenciárias`/`Trabalhistas, Sociais e Previdenciárias`/`Trabalhista e Previdenciária` → **Trabalhista e Previdenciária** (73, 7 Seguradoras); `Obrigações Fiscais` → **Obrigações Fiscais** (14). Ignoradas: `teste`, `teste 4`, `teste de taxa`, `super teste`, `Rafael teste`, `multa teste`, `Multa2a`, `Cobertura Teste Retorno Nulo`, `asdf`, `dsfasdf`, `sdfsdd`.
- **Jornada demonstrada ponta a ponta:** `GET /api/v1/modalities/{Licitante}/additional-coverages` com Escopo ativo da Finn devolve **`Multas`** e **`Trabalhista e Previdenciária`** — a oferta da etapa 3 sai do catálogo curado, derivada dos vínculos ativos (RN-104/RN-046).
- **Decisões de catálogo do Administrador (2026-08-04), 2ª rodada:** `Fiscal e Tributário` → **Obrigações Fiscais** (8 linhas); `MULTAS E PENALIDADES` (1) e `Adicional de Multas e Penalidades` (3) → **Multas**. O restante sem canônica correspondente foi **Ignorado**: `Monitoramento` (30), `Indenização Step In` (19), `Despesas com Contenção e Salvamento` (1), `reposição` (1) — reversível pelo `restore` se o catálogo canônico ganhar essas coberturas.
- **Fechamento da curadoria:** `Multas` 87 linhas / 7 Seguradoras; `Trabalhista e Previdenciária` 73 / 7; `Obrigações Fiscais` 22 / 2; Ignoradas 67; **pendentes 0**.
- **Oferta derivada, verificada por modalidade** (endpoint real, Escopo ativo da Finn): `Licitante` → `Multas | Trabalhista e Previdenciária`; `Adiantamento de Pagamento` e `Completion Bond` → `Multas | Obrigações Fiscais | Trabalhista e Previdenciária`. A lista varia com a Modalidade, como RN-046 exige — não é lista fixa no cliente.
