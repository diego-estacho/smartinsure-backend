# Tech debt tracker

Dívida técnica aceita conscientemente é registrada aqui com dono e gatilho de revisão (processo em [PLANS.md](../PLANS.md)).

| ID | Descrição | Motivo de aceitar | Dono | Gatilho de revisão |
|---|---|---|---|---|
| TD-001 | `check-harness.py` e `doctor.py` duplicados nos dois repos, sem módulo compartilhado | repos separados clonados isoladamente no CI (ADR-001) impedem import cruzado; custo de manter em sincronia é baixo hoje | harness (Diego) | 3ª divergência entre os lints (regra das 3 ocorrências) ou surgimento de um 3º repo |
| TD-002 | Roles vindas do token do IdP honradas pela policy (`JwtOptions.RoleClaimType`) — segundo caminho de confiança pro perfil Administrador do Sistema além do Profile da plataforma | RN-011/RN-012 não resolvem ambiguidade de fonte de verdade; comportamento preservado por compatibilidade com deploy existente | backend | antes de qualquer configuração de roles no Casdoor — decidir se deixa de honrar roles de token OU registra em SECURITY/ADR que Casdoor nunca emite essa role |
| TD-003 | Invariante do último admin (RN-012) é check-then-act sem serialização — duas revogações concorrentes podem zerar os admins | recuperável pelo runbook; risco baixo enquanto houver procedimento manual; troca necessária só com múltiplos admins ativos em produção | backend | quando houver mais de um admin ativo em produção |
| TD-004 | Corrida de unicidade de CNPJ devolve 500 (`DbUpdateException` do índice único) em vez de 409 | integridade garantida pelo banco; erro raro em produção pois é duplicação intencional; global de `DbUpdateException` deve cobrir futuros índices únicos | backend | primeiro caso real ou tratamento global de DbUpdateException |
| TD-005 | `ListAsync` de Seguradoras ordena só por `CorporateName` — paginação não-determinística em empates | comportamento correto em volume pequeno de dados; necessário quando temos muitas seguradoras com nomes idênticos | backend | junto da próxima mudança na listagem — adicionar `.ThenBy(Id)` |
| TD-006 | Aba Produção da Corretora sem fonte de dados — métricas (cotações, apólices ativas, prêmio, sinistros, conversão) dependem de domínios inexistentes | domínios Cotação/Apólice/Sinistro/Prêmio ainda não implementados e cotar/emitir é OPEN-07 (dono PO); front mostra estado vazio honesto, sem número falso (exec-plan 0009) | produto/PO + backend | quando os domínios de cotação/emissão existirem (após OPEN-07) — implementar `GET /brokerages/{id}/production` e ligar a aba |
| TD-007 | E2E Playwright das jornadas de Corretoras (filtros combinados, paginação, cadastro-não-persiste, validação da habilitação) não automatizado em CI — apenas verificado ao vivo (screenshots) | o harness de dev-auth do front (`/api/auth/dev-login`) grava um token sintético que o BFF repassa como `Bearer` mas o backend não aceita, e as páginas buscam dados no SSR (page.route do browser não intercepta) — automatizar exige mock-backend ou costura da sessão dev-auth→backend, fora do escopo do redesign; cobertura de comportamento garantida por unit tests (exec-plan 0009) | front (Diego) | próxima tarefa de front que toque auth/E2E, ou quando o harness dev-auth passar a emitir sessão aceita pelo backend — então habilitar os specs em `tests/e2e/` |
| TD-011 | Abas por Escopo da tela de Perfis de acesso (Todos·Sistema·Corretora·Tomador) sem **badge de contagem** — `GET /profiles` não devolve facets por Escopo | a contagem por Escopo exige uma resposta nova (o `PagedResponse<T>` é genérico e compartilhado) e a regra de visibilidade da RN-072 por Escopo; o núcleo da tela (editor, listagem, detalhe, exclusão-com-migração) entrega sem os números, e as abas funcionam sem badge; exec-plan 0019, jornada Perfis de acesso | backend + front (Diego) | quando o badge for pedido — adicionar `GET /profiles/scope-counts` (respeitando RN-072) e ligar os badges das abas |
| TD-010 | O front escolhe entre três endpoints de criação de Usuário (`/users/policy-holder-administrators`, `/users/brokerage-users`, `/users/policy-holder-users`) olhando o Escopo do Perfil e a presença de Corretora ativa — conhecimento da hierarquia RN-068/069/070 replicado no cliente | não há brecha de autorização (cada endpoint é fail-closed e recusa o que não é da alçada do solicitante), e a alternativa — um `POST /users` único que resolve o fluxo pelo Perfil informado — é mudança de contrato que o dono do produto precisa aprovar; achado HIGH do eixo Standards no code review de 2026-07-30, reclassificado para dívida por não ser falha de segurança | backend + dono do produto | quando o dono aprovar unificar a criação de Usuário num endpoint só (ele já sinalizou preferir "tudo na criação de usuário") — então o front volta a mandar apenas nome, e-mail, perfil e, quando houver, tomador |
| ~~TD-008~~ **RESOLVIDA** (2026-07-30) | ~~`IX_Profiles_Name` é único global~~ — substituído por três índices únicos **filtrados por Escopo** (`IX_Profiles_Name_System`, `_Brokerage` por `(BrokerageId, Name)`, `_PolicyHolder` por `(PolicyHolderId, Name)`) na migration `V20260730060026`, com o mapping EF alinhado 1:1. Duas Corretoras podem ter "Operador". Nota de operação: índice **filtrado** exige `SET QUOTED_IDENTIFIER ON` — aplicar por sqlcmd sem `-I` falha com "SET options have incorrect settings" | resolvido na fatia de Perfis customizados (RN-069/RN-070) | backend | — |
| TD-008 (histórico) | `IX_Profiles_Name` é único global; a fatia 0 (exec-plan 0008 de perfis) só tem Escopo System, então basta | RN-069/RN-070 exigem nome de Perfil único **por Escopo** (Corretora/Tomador) — dois escopos poderão ter "Operador" | backend | fatia 4 da jornada Perfis e Permissões — migration nova substituindo por índice composto/filtrado por `(Scope, BrokerageId/PolicyHolderId, Name)` |
| ~~TD-009~~ **PARCIAL** (2026-07-30) | O gatilho previsto (3º fluxo de criação — RN-068/RN-069) chegou e o provisionador compartilhado foi extraído: `IInvitedUserService` (identidade + Usuário + Convite + Vínculos + compensação + e-mail) atende os fluxos novos. **Os dois antigos seguem duplicados por decisão explícita do dono do produto** (não refatorar `CreateUser`/`InviteBrokerageAdministrator` nesta rodada) | duplicação restante limitada a 2 chamadores já cobertos por teste; o miolo novo não se multiplica mais | backend + dono do produto | quando o dono liberar tocar os dois fluxos antigos — trocá-los pelo `InvitedUserService` e fechar a dívida |
| TD-009 (histórico) | Fluxo criação+identidade+convite duplicado entre `CreateUserUseCase` e `InviteBrokerageAdministratorUseCase` (só a composição do e-mail foi extraída pro `InvitationMailer`) | a extração do provisionamento compartilhado (identidade+usuário+convite+compensação) é refactor de fluxo com risco em ship; os dois estão cobertos por teste; duplicação tolerável enquanto forem 2 chamadores | backend | 3º fluxo de criação de usuário (ex.: RN-070 Tomador Administrador cria Tomador) — extrair um provisionador compartilhado então |
