namespace SmartInsure.Application.UseCase.UseCases.PermissionUseCases.ListPermissions.Requests;

/// <summary>RN-063: o catálogo é o mesmo para todos — a consulta não tem filtro.</summary>
public sealed record ListPermissionsRequest;
